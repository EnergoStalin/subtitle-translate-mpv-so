[package]
name = "subtitle-translate-mpv-so"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
mpv = { version = "0.1.0", path = "../mpv" }
ffi = { version = "0.1.0", path = "../ffi" }

[build-dependencies]
xmake = "0.3.3"